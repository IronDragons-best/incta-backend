export enum AuthProvider {
  LOCAL = 'local',
  GOOGLE = 'google',
  GITHUB = 'github',
}

export enum OauthTemplateType {
  ADD_PROVIDER = 'user.provider.added',
  REGISTER_PROVIDER = 'user.provider.registered',
}
